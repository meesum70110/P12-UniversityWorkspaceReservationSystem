// Importing the CSS styles for the loading icon component
import './sharedComponents.css';

// Functional component definition for the Loading Icon
const LoadingIcon = (prop) => {

    return (
        <span>
            {/* Main wrapper for the loading spinner */}
            <div className="loadingio-spinner-rolling-2by998twmg8">
                <div className="ldio-yzaezf3dcmj">
                    {/* Inner div for the animated rolling spinner */}
                    <div></div>
                </div>
            </div>
        </span>
    );
};

// Exporting the LoadingIcon component as the default export
export default LoadingIcon;
